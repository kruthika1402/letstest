name: Simple Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello from the developer workflow"

      - name: Run Test
        run: echo "Running tests"

      - name: Finish
        run: echo "Workflow completed"
